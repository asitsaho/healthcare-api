"""Example feature slice.

Copy this directory to add a feature: models, schemas, service, router and
exceptions for one concern live together, rather than being smeared across five
top-level layer directories.

Remember to register new models in ``db/base.py`` so Alembic can see them.
"""
